package ArrayExamples;

/**
 * @author Guillermo Vigueras
 *
 */
public class Example2 {
	
    /**
     * Returns true if v1 is equal to v2 (element-wise). False otherwise 
     *
     * @param  v1    the first array of strings to compare
     * @param  v2    the second array of strings to compare
     * @return true if v1 is equal to v2 (element-wise). False otherwise
     */
    
	public boolean compareArrays(String [] v1, String [] v2) {
		  return false;
		}
	
	
}
	
